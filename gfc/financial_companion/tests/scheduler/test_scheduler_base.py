from ..test_base import BaseTestCase


class SchedulerTestCase(BaseTestCase):
    """
    Base class for testing schedulers.
    Call super().setUp() in the
    setUp() method of the subclass.
    """
